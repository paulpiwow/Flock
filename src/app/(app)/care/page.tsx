import { ComingSoon } from "@/components/ComingSoon";

export default function CarePage() {
  return (
    <ComingSoon
      title="Care Notes"
      phase="Phase 3"
      blurb="Dated notes on each guy across the semester; the RS sees the full timeline for summaries."
    />
  );
}
