import { ComingSoon } from "@/components/ComingSoon";

export default function VersePage() {
  return (
    <ComingSoon
      title="Memory Verse"
      phase="Phase 4"
      blurb="The verse of the week to memorize — plain reference and text, never interpreted."
    />
  );
}
