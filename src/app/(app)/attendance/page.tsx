import { ComingSoon } from "@/components/ComingSoon";

export default function AttendancePage() {
  return (
    <ComingSoon
      title="Attendance"
      phase="Phase 2"
      blurb="Tap names on your roster; the RS sees it all-hall, alphabetized, absentees on top."
    />
  );
}
