import { ComingSoon } from "@/components/ComingSoon";

export default function TrendsPage() {
  return (
    <ComingSoon
      title="Attendance Trends"
      phase="Phase 5"
      blurb="Attendance over time, per group and overall — spot a group slipping and step in early."
    />
  );
}
